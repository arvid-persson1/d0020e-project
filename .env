# 1. The Admin string (Used ONLY by the diesel CLI in your terminal)
DATABASE_URL=postgres://postgres@localhost:5632/bookery_db

# 2. The Read-Only string (Used ONLY by your Data Broker Connector)
MOCK_DB_URL=postgres://mock_reader@localhost:5632/bookery_db
